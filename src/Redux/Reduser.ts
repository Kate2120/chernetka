
import {Action} from './interfacies';

const initialState  = {
    users: [],
}
const Reduser = (state = initialState, action: Action) => {
switch(action.type){
    default:
      return state;
}
}
export default Reduser;